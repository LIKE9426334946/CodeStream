{
  "name": "codestream",
  "version": "1.0.0",
  "private": true,
  "description": "A personal, mobile-first code flow review app with a desktop editor.",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "NODE_ENV=development node --watch backend/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
